// const images = {};

// function importAll (r) {
//   r.keys().forEach(key => images[key] = r(key));
// }

// importAll(require.context('../assets/', true, /\.(png|svg|jpe?g|gif)$/));
// export default Object.values(images).map(mod=>`./${mod.default}`);
